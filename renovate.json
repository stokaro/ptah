{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "ignorePaths": [
    "**/node_modules/**",
    "**/bower_components/**",
    "**/vendor/**",
    "**/examples/**",
    "**/__tests__/**",
    "**/tests/**",
    "**/__fixtures__/**"
  ],
  "postUpdateOptions": [
    "gomodTidy"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "A database image pinned in a workflow's `docker run` is invisible to every built-in manager: the github-actions manager reads `uses:`, `container:` and `services:`, and the image here is an operand inside a `run:` shell block. Without this the docker-compose manager moved docker-compose.yaml alone and check-compose-image-pins.sh failed on the bot's own pull request, because that gate requires every Compose pin to be pinned by a workflow too -- a tag no workflow pins is a tag nothing tests. The marker comment names the image explicitly and sits directly above its own `docker run`, which is what ties the captured tag to it: Renovate evaluates these with RE2, which has no backreferences, so `\\k<depName>` fails to compile and stops the bot outright (stokaro/ptah#2339). Proximity plus a lazy gap reaches the first image-shaped token after the marker, and requiring that token to START WITH A LETTER is what keeps it off the `--publish 26257:26257` line one row above -- a published port is digits, and it was the reason the tie had to be exact in the first place. Grouped with the compose bump by the database-server-images rule below, which matches on the same depName, so both pins move in one pull request and the gate is green on arrival.",
      "managerFilePatterns": [
        "/^\\.github/workflows/.*\\.ya?ml$/"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[a-z-]+) depName=(?<depName>\\S+)[\\s\\S]*?\\n\\s*[a-z][\\w./-]*:(?<currentValue>[^\\s\\\\]+)"
      ]
    }
  ],
  "packageRules": [
    {
      "description": "The Go toolchain is declared once, as the `toolchain` directive in go.mod, and the built-in gomod manager proposes bumps for it without configuration. There is deliberately no customManager for a Go version: the one that used to live here could only reach .golangci.yml (its second file pattern, go/.golangci.yml, never existed), so a golang bump moved that file alone while fifteen other declarations stayed behind. The `go` directive is a published compatibility floor for two separately released import paths and moves on a human decision, not a bot's.",
      "matchDatasources": [
        "golang-version",
        "docker"
      ],
      "groupName": "golang version",
      "matchPackageNames": [
        "/^golang$/",
        "/^go$/"
      ]
    },
    {
      "description": "A database server image is not an ordinary dependency. Its tag names a release LINE this repository declares in internal/capabilityprobe/cells.go, carrying a capability preset, a support level and -- for a certified line -- a compatibility commitment, so replacing a tag can retire coverage rather than update a dependency. Classify the release before merging. A new LTS or stable line is a candidate for an additional cell, not a replacement for an existing one. A new innovation or current line is an early-signal target. A patch inside a line the matrix already declares is an ordinary update. A line reaching upstream end-of-life is a support-level decision -- legacy-tested if it is worth keeping as a regression sentinel, retired if it is not -- and never an automatic removal, because upstream end-of-life is not Ptah incompatibility. Never auto-merge one of these, and never let a newer image displace a declared line without deciding what happens to that line's cell. TestRenovate_ClassifiesEveryDatabaseServerImage keeps this package list and the declared cells in step.",
      "matchDatasources": [
        "docker"
      ],
      "matchPackageNames": [
        "postgres",
        "mysql",
        "mariadb",
        "clickhouse/clickhouse-server",
        "cockroachdb/cockroach",
        "yugabytedb/yugabyte",
        "gvenzl/oracle-free",
        "gvenzl/oracle-xe",
        "mcr.microsoft.com/mssql/server",
        "gcr.io/cloud-spanner-pg-adapter/pgadapter-emulator"
      ],
      "groupName": "database server images",
      "automerge": false,
      "addLabels": [
        "database-release-line"
      ]
    },
    {
      "description": "The clickhouse-legacy integration service is pinned below 24.11 deliberately: CHECK GRANT catalog visibility arrived in 24.11, and TestWriterDropDatabaseRealm_RejectsLegacyServerLive proves the realm-drop guard fails closed without it. Matching on the current value freezes only that pin and leaves the current clickhouse service free to update.",
      "matchDatasources": [
        "docker"
      ],
      "matchPackageNames": [
        "clickhouse/clickhouse-server"
      ],
      "matchCurrentValue": "24.10",
      "enabled": false
    }
  ]
}
