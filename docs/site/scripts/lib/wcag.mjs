// The WCAG levels the browser checks hold the site to, as axe tags. One list,
// so the page-wide check and the version picker's check judge by the same
// rules.
export const WCAG_TAGS = ['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa'];
