---
title: Glossary
description: Ptah's product, workflow, database-support, and compatibility terms, with the file that pins each one.
type: reference
audience:
  - "all-users"
readerQuestion: "What do Ptah's product and support terms mean?"
goal: "Look up Ptah's product and support terms."
sourceOfTruth:
  - "cmd"
  - "core"
  - "migration"
generated: false
overlaps: []
disposition: keep
---

import GlossaryTerm from '../../../components/GlossaryTerm.astro';
import GlossaryList from '../../../components/GlossaryList.astro';

Every term below carries its definition, the page that uses it, and where a
definition is pinned, the file that decides it. Definitions come from one
source, `docs/site/src/glossary.ts`, so the same term cannot come to mean two
different things on two pages.

Where a dense table elsewhere cannot carry a definition, the same entry appears
beside the term as an inline ⓘ: focus it with <kbd>Tab</kbd> or tap it, and
press <kbd>Esc</kbd> to dismiss.

<GlossaryList />

## Inside a table that scrolls

This is the case the inline affordance is most likely to break, so it is
exercised here rather than assumed. The table below is five columns wide and
scrolls horizontally on a phone; every cell in the last column carries a term.
The `exampledb` rows are fictional and do not state current Ptah support.

| Dialect | Line | Support | Probe | Note |
| --- | --- | --- | --- | --- |
| exampledb | 99.1 | <GlossaryTerm term="certified" /> | every pull request | <GlossaryTerm term="measured">measured on this line</GlossaryTerm> |
| exampledb | 98.0 | <GlossaryTerm term="legacy-tested" /> | every pull request | <GlossaryTerm term="measured">measured on this line</GlossaryTerm> |
| exampledb | 97.0 | <GlossaryTerm term="best-effort" /> | none | <GlossaryTerm term="carried">carried from 98.0</GlossaryTerm> |
| exampledb | 96.0 | <GlossaryTerm term="known-incompatible" /> | none | <GlossaryTerm term="carried">carried from 97.0</GlossaryTerm> |

## Adding a term

Add it to `docs/site/src/glossary.ts` with its `group`, the `learnMore` page
that teaches it, and a `source` where a file in the repository pins the fact.
The list above renders from that map, so the term appears here without being
transcribed, and `check-glossary-coverage.mjs` refuses an entry that names a
page which does not exist.

Then use `<GlossaryTerm term="…" />` wherever a table shows the term as a glyph
or a number whose meaning lives elsewhere on the page. A term with no entry in
the map is a build error rather than a silent blank, so a typo cannot ship as an
empty tooltip.
