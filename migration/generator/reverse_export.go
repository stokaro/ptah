package generator

// Reverse planning is exposed only through PlanBidirectionalSchemaDiff. The
// pre-v1 structural-only ReverseSchemaDiff function is intentionally not kept
// as a compatibility wrapper.
